<!DOCTYPE html>
<html>
<head>
  <title>Admin Login</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="container admin-login-page">
    <nav class="topbar">
      <h1 class="brand">Admin Login</h1>
      <div class="menu">
        <a href="/">Back to Store</a>
      </div>
    </nav>

    <section class="panel" style="max-width:520px;margin:24px auto 0;">
      <form action="/admin/login" method="POST" class="stack">
        <div>
          <label>Username</label>
          <input type="text" name="username" required>
        </div>
        <div>
          <label>Password</label>
          <input type="password" name="password" required>
        </div>
        <button type="submit">Login</button>
      </form>
    </section>
  </div>
  <% if (adminWhatsAppNumber) { %>
    <a
      class="floating-whatsapp"
      href="https://wa.me/<%= adminWhatsAppNumber %>?text=<%= encodeURIComponent('Hello Admin') %>"
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Chat on WhatsApp"
      title="Chat on WhatsApp"
    >WA</a>
  <% } %>
</body>
</html>
